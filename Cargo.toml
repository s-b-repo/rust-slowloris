[package]
name    = "rsslowloris"
version = "0.0.1"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
clap  = { version = "4", features = ["derive"] }
rand  = "0.8"
anyhow = "1"      # convenient error handling
